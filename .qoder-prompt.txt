分析并修复以下 Token 统计相关代码问题（项目路径 /Users/renlinggao/workspace/ai-agent）：

先读取所有相关代码文件，逐一检查以下问题是否存在，如果存在就修复：

1. **AgentService.java — onUsage 回调时机**
   验证 AgentService 内部匿名 LlmStreamListener 的 onUsage 是否被正确覆写，以及 listen() 的 onComplete 内 onUsage → pushUsage → pushComplete 的顺序是否正确。

2. **chat.js — updateTokenUsage 累加逻辑**
   当前代码 `totalUsedTokens += (usage.promptTokens || 0) + (usage.completionTokens || 0)`，建议改为 `totalUsedTokens += usage.totalTokens || 0` 避免可能的重复计算。检查并修改。

3. **chat.js — clearMessages 没有重置 totalUsedTokens**
   检查 clearMessages、createNewSession、switchSession 函数是否重置了 `totalUsedTokens = 0` 并调用 `updateTokenProgressRing()`。缺失则补上。

4. **chat.js — token-stats 插入位置**
   检查 `updateTokenUsage` 中插入 `.token-stats` 的方式，确认位置是否正确。如果 assistant 消息后面有 tool 消息，token 统计不应该夹在中间，建议改成 appendChild 到 messagesArea 末尾或确认当前逻辑无问题。

5. **ChatPanel.java — pushUsageToJs 参数拼接方式**
   当前 `pushToJs("updateTokenUsage", json + "," + elapsedMs)` 可读性差，改为统一 JSON 对象：`{"usage":{...}, "elapsedMs": N}`。

6. **chat.css — 进度条 tooltip 位置**
   检查 tooltip 在小屏幕上的显示问题，适当加 max-width 或改用 right:0 对齐。

7. **进度条颜色变化**
   当 totalUsedTokens 接近 200K（> 80%）时，进度条颜色从 accent 变橙色/红色，给视觉提示。

改完后记得编译验证。编译通过后不要做其他操作，等我下一步指示。